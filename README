This is Perl module JSON::RPC2::Server::Declare.

INSTALLATION

JSON::RPC2::Server::Declare installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan JSON::RPC2::Server::Declare

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

JSON::RPC2::Server::Declare documentation is available as in POD. So you can do:

    % perldoc JSON::RPC2::Server::Declare

to read the documentation online with your favorite pager.

ytnobody
